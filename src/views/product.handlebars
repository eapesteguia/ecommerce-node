<div class="text-center my-4">
    <label>Pistachos y frutos secos sanjuaninos</label>
    <h1>Detalle de producto</h1>
</div>

<section class="d-flex justify-content-center align-items-center"> 
        <div class="card text-center" style="width: 22rem;">
        <img src="{{this.thumbnails}}" class="card-img-top" alt="{{this.title}}">
            <div class="card-header">
                Precio final: ${{this.price}}
            </div>
            <div class="card-body">
                <h4 class="card-title">{{this.title}}</h4>
                <p class="card-text">{{this.description}}</p>
                <a id="addToCart" class="btn btn-success" data-id="{{this._id}}" href="#">Agregar al carrito</a>
            </div>

            <ul class="list-group list-group-flush">
    <li class="list-group-item">Categoría: {{this.category}}</li>
    <li class="list-group-item">Stock: {{this.stock}} unidades</li>
            </ul>
        </div>
</section>

<footer class="text-center my-4">
    <a class="btn btn-secondary" href="http://localhost:8080"><< Volver </a>
</footer>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/product.js"></script>
